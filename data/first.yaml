_data:
  title: "First!"
_snippets:
  main: |
    #include <iostream>

    //TODO: Stuff!
    class Thing {
        // https://example.com
    };

    int main () {
        int x = 0;
        std::cout << x << '\n';

        std::cout << "hello world\n";

        return 0;
    }
  func: |
    template<typename T>
    T func (T a, T b) {
      return a + b;
    }